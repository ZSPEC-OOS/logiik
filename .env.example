# Teacher AI API Key (OpenAI or Anthropic)
TEACHER_API_KEY=your-api-key-here

# Teacher provider: openai or anthropic
TEACHER_PROVIDER=openai

# Local brain storage path (weights, checkpoints, vectors)
KNOWLEDGE_BASE_PATH=./knowledge_base

# Firebase cloud memory (leave blank to run local-only)
FIREBASE_CREDENTIAL_PATH=/path/to/serviceAccountKey.json
FIREBASE_BRAIN_ID=default

# API server settings
API_HOST=0.0.0.0
API_PORT=8000
