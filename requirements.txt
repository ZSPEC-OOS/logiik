# ─── Logiik Requirements ────────────────────────────────────────────────────
# Core ML / Training
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.6
sentence-transformers>=2.7.0
accelerate>=0.29.0
bitsandbytes>=0.43.0

# Scientific Embeddings
# allenai/specter2 is loaded via sentence-transformers — no extra install needed

# Image Understanding
Pillow>=10.0.0
pytesseract>=0.3.10

# PDF Processing
PyMuPDF>=1.24.0

# Vector Database
pinecone-client>=3.2.0

# Firebase (REST only — no Admin SDK required)
requests>=2.31.0

# Redis (installed but disabled via config flag)
redis>=5.0.0

# AWS S3 (installed but disabled via config flag)
boto3>=1.34.0

# Scientific / Numerical
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0
networkx>=3.3.0

# Web Framework / API
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0
pydantic>=2.6.0

# Dashboard
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Utilities
tqdm>=4.66.0
python-multipart>=0.0.9
